Run Matveev and company's Spine, aka The 3-manifold Recognizer,
progammatically by driving the Windows GUI using pywinauto.

Install via the following command in this directory::

  python -m pip install .

and then look at the `test.py` script for a simple example.
